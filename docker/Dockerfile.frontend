FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .

EXPOSE 5173

# --host 0.0.0.0 is required for Docker port binding to work in WSL
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
